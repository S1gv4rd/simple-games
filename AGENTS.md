# simple-games

Educational games for children. Next.js (App Router) + TypeScript.

## Commands

```bash
npm run dev      # http://localhost:3000
npm run build
npm start
npm run lint
```

## Structure

- Route-per-game: `app/[game]/page.tsx`
- Shared UI in `components/`
